# Copy to .env at the repo root (backend reads it from there).

# Langfuse observability (optional — app runs without it, health endpoint shows status)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# Coming in later phases:
# OPENROUTER_API_KEY=
# DEEPGRAM_API_KEY=
